package pm;

import am.A;
//import am.F; default로 선언한 F 클래스는 import 할 수 없다.
//public으로 선언된 클래스만 import 가능하다.

public class Exam7_1 {
	public static void main(String[] args) {
		//클래스의 접근 지정자.
		//클래스는 public, default 접근 지정자만 사용할 수 있다.
		//default 클래스는 같은 패키지에서만
		//public 클래스는 다른 패키지에서도 사용가능
		//즉, 다른 패키지에서 import 가능 유무차이
		
	}
}
